Calculator_V1.0 - выполнено согласно ТЗ. В методе GET передается параметр expr c математическим выражением.
Однако не сказано может ли быть это выражение некорректным или с лишними символами пробела.
Саму строку из параметра expr мы можем получает через reaqest.args('expr'), однако в данном случае символы '+'
заменяются на пробелы. Поэтому проще на мой взгляд получить строку-выражение из URL.

Пример url для выполнения программы: http://127.0.0.1:5000/?expr=math.sqrt(math.pow(10,%202)+math.pow(15,%202))

Calculator_V1.1 - ввод и вывод данных реализован через формы, с добавлением метода POST.